//Primitive types
const object = {key: value} //object (call on by object.key)
const name = "siera ford" //string
const function = (arg) => {
return arg
};//function
const array = ["squirtle", "wartortle", "blastoise"] //array (call on by array[0])
const integer = 496 //integer
const boolean = true //boolean (can be true or false)
const date = new Date //date
//all of above are variables but with different values





array[0]
const object2 = {poke1: "squirtle", poke2: "wartortle", poke3: "blastoise"}//another object
